import { HubbleImageDetail } from './hubble-image-detail';

describe('HubbleImageDetail', () => {
  it('should create an instance', () => {
    expect(new HubbleImageDetail()).toBeTruthy();
  });
});
