import { HubbleVideosDetail } from './hubble-videos-detail';

describe('HubbleVideosDetail', () => {
  it('should create an instance', () => {
    expect(new HubbleVideosDetail()).toBeTruthy();
  });
});
